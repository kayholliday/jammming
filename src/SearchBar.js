import React from "react";

//send input data to ? to get the results


function SearchBar() {
    return (
        <>
            <div>
                <button id='search' name='search'>Search</button>
                <input type='text'></input>
            </div>
        </>
    )
}

export default SearchBar;